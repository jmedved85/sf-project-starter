{% extends 'base.html.twig' %}

{% block title %}{{ 'home'|trans }}{% endblock %}

{% block content %}
    <div class="col-md-12">
        <div class="d-flex justify-content-between align-items-center mx-3">
            <h1 class="display-6">
                <span class="d-none d-lg-inline">{{ 'welcome_home'|trans }}</span>
            </h1>
        </div>

        <hr>

        <div class="container mt-5">
            <div class="row">
                <div class="col-md-12">
                    <p class="lead">{{ 'home_page_description'|trans }}</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}